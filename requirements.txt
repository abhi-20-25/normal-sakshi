# Requirements for the CCTV / YOLO Flask server project
# For Ubuntu server deployment at ubuntu@13.203.73.173
# Note: Run system package installation first (see README.md)

# Core ML / vision (CPU versions for server deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu
ultralytics==8.0.196

# OpenCV (headless for server deployment)
opencv-python-headless==4.8.1.78

# Numerical & image I/O
numpy>=1.23.0
Pillow>=9.0.0
imageio>=2.9.0
imageio-ffmpeg>=0.4.7

# Web / server
Flask>=2.2.0
flask-socketio>=5.0.0
python-socketio>=5.0.0
eventlet>=0.33.0

# Database / ORM
SQLAlchemy>=1.4.0

# Geometry, scheduling, utilities
Shapely>=2.0.0
APScheduler>=3.9.0
pytz>=2022.1
requests>=2.28.0

# Helpful data tooling (optional but commonly needed by ultralytics)
pandas>=1.5.0

# Optional: torchvision if you need transforms / pretrained model utilities
torchvision>=0.14.0 ; extra == 'torchvision'
